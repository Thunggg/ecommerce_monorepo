meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/categories/{{categoryId}}
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  categoryId: 1
}
