meta {
  name: Get by id
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/brand-translations/{{brandTranslationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  brandTranslationId: 4
}
