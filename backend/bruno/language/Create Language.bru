meta {
  name: Create Language
  type: http
  seq: 31
}

post {
  url: {{baseUrl}}/language
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "id": "en",
    "name": "English"
  }
}
