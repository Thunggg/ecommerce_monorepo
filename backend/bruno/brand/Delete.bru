meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/brands/{{brandId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

vars:pre-request {
  brandId: 1
}
