meta {
  name: Get Orders
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/orders?page=1&limit=10
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{accessToken}}
}